.TH "pip._internal.resolution.resolvelib.found_candidates" 3 "Version 1.0" "Minilog" \" -*- nroff -*-
.ad l
.nh
.SH NAME
pip._internal.resolution.resolvelib.found_candidates
.SH SYNOPSIS
.br
.PP
.SS "Classes"

.in +1c
.ti -1c
.RI "class \fBFoundCandidates\fP"
.br
.in -1c
.SS "Functions"

.in +1c
.ti -1c
.RI "Iterator[\fBCandidate\fP] \fB_iter_built\fP (Iterator[\fBIndexCandidateInfo\fP] infos)"
.br
.ti -1c
.RI "Iterator[\fBCandidate\fP] \fB_iter_built_with_prepended\fP (\fBCandidate\fP installed, Iterator[\fBIndexCandidateInfo\fP] infos)"
.br
.ti -1c
.RI "Iterator[\fBCandidate\fP] \fB_iter_built_with_inserted\fP (\fBCandidate\fP installed, Iterator[\fBIndexCandidateInfo\fP] infos)"
.br
.in -1c
.SS "Variables"

.in +1c
.ti -1c
.RI "\fBlogger\fP = logging\&.getLogger(__name__)"
.br
.ti -1c
.RI "\fBIndexCandidateInfo\fP = Tuple[\fB_BaseVersion\fP, Callable[[], Optional[\fBCandidate\fP]]]"
.br
.ti -1c
.RI "\fBSequenceCandidate\fP = Sequence[\fBCandidate\fP]"
.br
.in -1c
.SH "Detailed Description"
.PP 

.PP
.nf
Utilities to lazily create and visit candidates found\&.

Creating and visiting a candidate is a *very* costly operation\&. It involves
fetching, extracting, potentially building modules from source, and verifying
distribution metadata\&. It is therefore crucial for performance to keep
everything here lazy all the way down, so we only touch candidates that we
absolutely need, and not 'download the world' when we only need one version of
something\&.

.fi
.PP
 
.SH "Function Documentation"
.PP 
.SS " Iterator[\fBCandidate\fP] pip\&._internal\&.resolution\&.resolvelib\&.found_candidates\&._iter_built (Iterator[\fBIndexCandidateInfo\fP] infos)\fC [protected]\fP"

.PP
.nf
Iterator for ``FoundCandidates``\&.

This iterator is used when the package is not already installed\&. Candidates
from index come later in their normal ordering\&.

.fi
.PP
 
.SS " Iterator[\fBCandidate\fP] pip\&._internal\&.resolution\&.resolvelib\&.found_candidates\&._iter_built_with_inserted (\fBCandidate\fP installed, Iterator[\fBIndexCandidateInfo\fP]
 infos)\fC [protected]\fP"

.PP
.nf
Iterator for ``FoundCandidates``\&.

This iterator is used when the resolver prefers to upgrade an
already-installed package\&. Candidates from index are returned in their
normal ordering, except replaced when the version is already installed\&.

The implementation iterates through and yields other candidates, inserting
the installed candidate exactly once before we start yielding older or
equivalent candidates, or after all other candidates if they are all newer\&.

.fi
.PP
 
.SS " Iterator[\fBCandidate\fP] pip\&._internal\&.resolution\&.resolvelib\&.found_candidates\&._iter_built_with_prepended (\fBCandidate\fP installed, Iterator[\fBIndexCandidateInfo\fP]
 infos)\fC [protected]\fP"

.PP
.nf
Iterator for ``FoundCandidates``\&.

This iterator is used when the resolver prefers the already-installed
candidate and NOT to upgrade\&. The installed candidate is therefore
always yielded first, and candidates from index come later in their
normal ordering, except skipped when the version is already installed\&.

.fi
.PP
 
.SH "Variable Documentation"
.PP 
.SS "pip\&._internal\&.resolution\&.resolvelib\&.found_candidates\&.IndexCandidateInfo = Tuple[\fB_BaseVersion\fP, Callable[[], Optional[\fBCandidate\fP]]]"

.SS "pip\&._internal\&.resolution\&.resolvelib\&.found_candidates\&.logger = logging\&.getLogger(__name__)"

.SS "pip\&._internal\&.resolution\&.resolvelib\&.found_candidates\&.SequenceCandidate = Sequence[\fBCandidate\fP]"

.SH "Author"
.PP 
Generated automatically by Doxygen for Minilog from the source code\&.
