.TH "urllib3._request_methods.RequestMethods" 3 "Version 1.0" "Minilog" \" -*- nroff -*-
.ad l
.nh
.SH NAME
urllib3._request_methods.RequestMethods
.SH SYNOPSIS
.br
.PP
.PP
Inherited by \fBurllib3\&.connectionpool\&.HTTPConnectionPool\fP, and \fBurllib3\&.poolmanager\&.PoolManager\fP\&.
.SS "Public Member Functions"

.in +1c
.ti -1c
.RI "None \fB__init__\fP (self, typing\&.Mapping[str, str]|None \fBheaders\fP=None)"
.br
.ti -1c
.RI "\fBBaseHTTPResponse\fP \fBurlopen\fP (self, str method, str url, _TYPE_BODY|None body=None, typing\&.Mapping[str, str]|None \fBheaders\fP=None, bool encode_multipart=True, str|None multipart_boundary=None, **typing\&.Any kw)"
.br
.ti -1c
.RI "\fBBaseHTTPResponse\fP \fBrequest\fP (self, str method, str url, _TYPE_BODY|None body=None, _TYPE_FIELDS|None fields=None, typing\&.Mapping[str, str]|None \fBheaders\fP=None, typing\&.Any|None json=None, **typing\&.Any urlopen_kw)"
.br
.ti -1c
.RI "\fBBaseHTTPResponse\fP \fBrequest_encode_url\fP (self, str method, str url, \fB_TYPE_ENCODE_URL_FIELDS\fP|None fields=None, typing\&.Mapping[str, str]|None \fBheaders\fP=None, **str urlopen_kw)"
.br
.ti -1c
.RI "\fBBaseHTTPResponse\fP \fBrequest_encode_body\fP (self, str method, str url, _TYPE_FIELDS|None fields=None, typing\&.Mapping[str, str]|None \fBheaders\fP=None, bool encode_multipart=True, str|None multipart_boundary=None, **str urlopen_kw)"
.br
.in -1c
.SS "Public Attributes"

.in +1c
.ti -1c
.RI "\fBheaders\fP"
.br
.in -1c
.SS "Static Protected Attributes"

.in +1c
.ti -1c
.RI "dict \fB_encode_url_methods\fP = {'DELETE', 'GET', 'HEAD', 'OPTIONS'}"
.br
.in -1c
.SH "Detailed Description"
.PP 

.PP
.nf
Convenience mixin for classes who implement a :meth:`urlopen` method, such
as :class:`urllib3\&.HTTPConnectionPool` and
:class:`urllib3\&.PoolManager`\&.

Provides behavior for making common types of HTTP request methods and
decides which type of request field encoding to use\&.

Specifically,

:meth:`\&.request_encode_url` is for sending requests whose fields are
encoded in the URL (such as GET, HEAD, DELETE)\&.

:meth:`\&.request_encode_body` is for sending requests whose fields are
encoded in the *body* of the request using multipart or www-form-urlencoded
(such as for POST, PUT, PATCH)\&.

:meth:`\&.request` is for making any kind of request, it will look up the
appropriate encoding format and use one of the above two methods to make
the request\&.

Initializer parameters:

:param headers:
    Headers to include with all requests, unless other headers are given
    explicitly\&.

.fi
.PP
 
.SH "Constructor & Destructor Documentation"
.PP 
.SS " None urllib3\&._request_methods\&.RequestMethods\&.__init__ ( self, typing\&.Mapping[str, str] | None  headers = \fCNone\fP)"

.PP
Reimplemented in \fBurllib3\&.poolmanager\&.PoolManager\fP, \fBurllib3\&.connectionpool\&.HTTPConnectionPool\fP, \fBurllib3\&.connectionpool\&.HTTPSConnectionPool\fP, \fBurllib3\&.poolmanager\&.ProxyManager\fP, and \fBurllib3\&.contrib\&.socks\&.SOCKSProxyManager\fP\&.
.SH "Member Function Documentation"
.PP 
.SS " \fBBaseHTTPResponse\fP urllib3\&._request_methods\&.RequestMethods\&.request ( self, str method, str url, _TYPE_BODY | None  body = \fCNone\fP, _TYPE_FIELDS | None  fields = \fCNone\fP, typing\&.Mapping[str, str] | None  headers = \fCNone\fP, typing\&.Any | None  json = \fCNone\fP, **typing\&.Any urlopen_kw)"

.PP
.nf
Make a request using :meth:`urlopen` with the appropriate encoding of
``fields`` based on the ``method`` used\&.

This is a convenience method that requires the least amount of manual
effort\&. It can be used in most situations, while still having the
option to drop down to more specific methods when necessary, such as
:meth:`request_encode_url`, :meth:`request_encode_body`,
or even the lowest level :meth:`urlopen`\&.

:param method:
    HTTP request method (such as GET, POST, PUT, etc\&.)

:param url:
    The URL to perform the request on\&.

:param body:
    Data to send in the request body, either :class:`str`, :class:`bytes`,
    an iterable of :class:`str`/:class:`bytes`, or a file-like object\&.

:param fields:
    Data to encode and send in the URL or request body, depending on ``method``\&.

:param headers:
    Dictionary of custom headers to send, such as User-Agent,
    If-None-Match, etc\&. If None, pool headers are used\&. If provided,
    these headers completely replace any pool-specific headers\&.

:param json:
    Data to encode and send as JSON with UTF-encoded in the request body\&.
    The ``'Content-Type'`` header will be set to ``'application/json'``
    unless specified otherwise\&.

.fi
.PP
 
.SS " \fBBaseHTTPResponse\fP urllib3\&._request_methods\&.RequestMethods\&.request_encode_body ( self, str method, str url, _TYPE_FIELDS | None  fields = \fCNone\fP, typing\&.Mapping[str, str] | None  headers = \fCNone\fP, bool  encode_multipart = \fCTrue\fP, str | None  multipart_boundary = \fCNone\fP, **str urlopen_kw)"

.PP
.nf
Make a request using :meth:`urlopen` with the ``fields`` encoded in
the body\&. This is useful for request methods like POST, PUT, PATCH, etc\&.

When ``encode_multipart=True`` (default), then
:func:`urllib3\&.encode_multipart_formdata` is used to encode
the payload with the appropriate content type\&. Otherwise
:func:`urllib\&.parse\&.urlencode` is used with the
'application/x-www-form-urlencoded' content type\&.

Multipart encoding must be used when posting files, and it's reasonably
safe to use it in other times too\&. However, it may break request
signing, such as with OAuth\&.

Supports an optional ``fields`` parameter of key/value strings AND
key/filetuple\&. A filetuple is a (filename, data, MIME type) tuple where
the MIME type is optional\&. For example::

    fields = {
        'foo': 'bar',
        'fakefile': ('foofile\&.txt', 'contents of foofile'),
        'realfile': ('barfile\&.txt', open('realfile')\&.read()),
        'typedfile': ('bazfile\&.bin', open('bazfile')\&.read(),
                      'image/jpeg'),
        'nonamefile': 'contents of nonamefile field',
    }

When uploading a file, providing a filename (the first parameter of the
tuple) is optional but recommended to best mimic behavior of browsers\&.

Note that if ``headers`` are supplied, the 'Content-Type' header will
be overwritten because it depends on the dynamic random boundary string
which is used to compose the body of the request\&. The random boundary
string can be explicitly set with the ``multipart_boundary`` parameter\&.

:param method:
    HTTP request method (such as GET, POST, PUT, etc\&.)

:param url:
    The URL to perform the request on\&.

:param fields:
    Data to encode and send in the request body\&.

:param headers:
    Dictionary of custom headers to send, such as User-Agent,
    If-None-Match, etc\&. If None, pool headers are used\&. If provided,
    these headers completely replace any pool-specific headers\&.

:param encode_multipart:
    If True, encode the ``fields`` using the multipart/form-data MIME
    format\&.

:param multipart_boundary:
    If not specified, then a random boundary will be generated using
    :func:`urllib3\&.filepost\&.choose_boundary`\&.

.fi
.PP
 
.SS " \fBBaseHTTPResponse\fP urllib3\&._request_methods\&.RequestMethods\&.request_encode_url ( self, str method, str url, \fB_TYPE_ENCODE_URL_FIELDS\fP | None  fields = \fCNone\fP, typing\&.Mapping[str, str] | None  headers = \fCNone\fP, **str urlopen_kw)"

.PP
.nf
Make a request using :meth:`urlopen` with the ``fields`` encoded in
the url\&. This is useful for request methods like GET, HEAD, DELETE, etc\&.

:param method:
    HTTP request method (such as GET, POST, PUT, etc\&.)

:param url:
    The URL to perform the request on\&.

:param fields:
    Data to encode and send in the URL\&.

:param headers:
    Dictionary of custom headers to send, such as User-Agent,
    If-None-Match, etc\&. If None, pool headers are used\&. If provided,
    these headers completely replace any pool-specific headers\&.

.fi
.PP
 
.SS " \fBBaseHTTPResponse\fP urllib3\&._request_methods\&.RequestMethods\&.urlopen ( self, str method, str url, _TYPE_BODY | None  body = \fCNone\fP, typing\&.Mapping[str, str] | None  headers = \fCNone\fP, bool  encode_multipart = \fCTrue\fP, str | None  multipart_boundary = \fCNone\fP, **typing\&.Any kw)"

.PP
Reimplemented in \fBurllib3\&.connectionpool\&.HTTPConnectionPool\fP, \fBurllib3\&.poolmanager\&.PoolManager\fP, and \fBurllib3\&.poolmanager\&.ProxyManager\fP\&.
.SH "Member Data Documentation"
.PP 
.SS "dict urllib3\&._request_methods\&.RequestMethods\&._encode_url_methods = {'DELETE', 'GET', 'HEAD', 'OPTIONS'}\fC [static]\fP, \fC [protected]\fP"

.SS "urllib3\&._request_methods\&.RequestMethods\&.headers"


.SH "Author"
.PP 
Generated automatically by Doxygen for Minilog from the source code\&.
