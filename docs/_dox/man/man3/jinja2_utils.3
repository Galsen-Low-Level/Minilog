.TH "jinja2.utils" 3 "Version 1.0" "Minilog" \" -*- nroff -*-
.ad l
.nh
.SH NAME
jinja2.utils
.SH SYNOPSIS
.br
.PP
.SS "Classes"

.in +1c
.ti -1c
.RI "class \fB_MissingType\fP"
.br
.ti -1c
.RI "class \fB_PassArg\fP"
.br
.ti -1c
.RI "class \fBCycler\fP"
.br
.ti -1c
.RI "class \fBJoiner\fP"
.br
.ti -1c
.RI "class \fBLRUCache\fP"
.br
.ti -1c
.RI "class \fBNamespace\fP"
.br
.in -1c
.SS "Functions"

.in +1c
.ti -1c
.RI "\fBF\fP \fBpass_context\fP (\fBF\fP f)"
.br
.ti -1c
.RI "\fBF\fP \fBpass_eval_context\fP (\fBF\fP f)"
.br
.ti -1c
.RI "\fBF\fP \fBpass_environment\fP (\fBF\fP f)"
.br
.ti -1c
.RI "\fBF\fP \fBinternalcode\fP (\fBF\fP f)"
.br
.ti -1c
.RI "bool \fBis_undefined\fP (t\&.Any obj)"
.br
.ti -1c
.RI "None \fBconsume\fP (t\&.Iterable[t\&.Any] iterable)"
.br
.ti -1c
.RI "None \fBclear_caches\fP ()"
.br
.ti -1c
.RI "t\&.Any \fBimport_string\fP (str import_name, bool silent=False)"
.br
.ti -1c
.RI "t\&.Optional[t\&.IO[t\&.Any]] \fBopen_if_exists\fP (str filename, str mode='rb')"
.br
.ti -1c
.RI "str \fBobject_type_repr\fP (t\&.Any obj)"
.br
.ti -1c
.RI "str \fBpformat\fP (t\&.Any obj)"
.br
.ti -1c
.RI "str \fBurlize\fP (str text, t\&.Optional[int] trim_url_limit=None, t\&.Optional[str] rel=None, t\&.Optional[str] target=None, t\&.Optional[t\&.Iterable[str]] extra_schemes=None)"
.br
.ti -1c
.RI "str \fBgenerate_lorem_ipsum\fP (int n=5, bool html=True, int min=20, int max=100)"
.br
.ti -1c
.RI "str \fBurl_quote\fP (t\&.Any obj, str charset='utf\-8', bool for_qs=False)"
.br
.ti -1c
.RI "t\&.Callable[[t\&.Optional[str]], bool] \fBselect_autoescape\fP (t\&.Collection[str] enabled_extensions=('html', 'htm', 'xml'), t\&.Collection[str] disabled_extensions=(), bool default_for_string=True, bool default=False)"
.br
.ti -1c
.RI "\fBmarkupsafe\&.Markup\fP \fBhtmlsafe_json_dumps\fP (t\&.Any obj, t\&.Optional[t\&.Callable[\&.\&.\&., str]] dumps=None, **t\&.Any kwargs)"
.br
.in -1c
.SS "Variables"

.in +1c
.ti -1c
.RI "\fBF\fP = t\&.TypeVar('F', bound=t\&.Callable[\&.\&.\&., t\&.Any])"
.br
.ti -1c
.RI "str \fBconcat\fP = ''\&.join"
.br
.ti -1c
.RI "\fB_http_re\fP"
.br
.ti -1c
.RI "\fB_email_re\fP = re\&.compile(r'^\\S+@\\w[\\w\&.\-]*\\\&.\\w+$')"
.br
.in -1c
.SH "Function Documentation"
.PP 
.SS " None jinja2\&.utils\&.clear_caches ()"

.PP
.nf
Jinja keeps internal caches for environments and lexers\&.  These are
used so that Jinja doesn't have to recreate environments and lexers all
the time\&.  Normally you don't have to care about that but if you are
measuring memory consumption you may want to clean the caches\&.

.fi
.PP
 
.SS " None jinja2\&.utils\&.consume (t\&.Iterable[t\&.Any] iterable)"

.PP
.nf
Consumes an iterable without doing anything with it\&.
.fi
.PP
 
.SS " str jinja2\&.utils\&.generate_lorem_ipsum (int  n = \fC5\fP, bool  html = \fCTrue\fP, int  min = \fC20\fP, int  max = \fC100\fP)"

.PP
.nf
Generate some lorem ipsum for the template\&.
.fi
.PP
 
.SS " \fBmarkupsafe\&.Markup\fP jinja2\&.utils\&.htmlsafe_json_dumps (t\&.Any obj, t\&.Optional[t\&.Callable[\&.\&.\&., str]]  dumps = \fCNone\fP, **t\&.Any
 kwargs)"

.PP
.nf
Serialize an object to a string of JSON with :func:`json\&.dumps`,
then replace HTML-unsafe characters with Unicode escapes and mark
the result safe with :class:`~markupsafe\&.Markup`\&.

This is available in templates as the ``|tojson`` filter\&.

The following characters are escaped: ``<``, ``>``, ``&``, ``'``\&.

The returned string is safe to render in HTML documents and
``<script>`` tags\&. The exception is in HTML attributes that are
double quoted; either use single quotes or the ``|forceescape``
filter\&.

:param obj: The object to serialize to JSON\&.
:param dumps: The ``dumps`` function to use\&. Defaults to
    ``env\&.policies['json\&.dumps_function']``, which defaults to
    :func:`json\&.dumps`\&.
:param kwargs: Extra arguments to pass to ``dumps``\&. Merged onto
    ``env\&.policies['json\&.dumps_kwargs']``\&.

\&.\&. versionchanged:: 3\&.0
    The ``dumper`` parameter is renamed to ``dumps``\&.

\&.\&. versionadded:: 2\&.9

.fi
.PP
 
.SS " t\&.Any jinja2\&.utils\&.import_string (str import_name, bool  silent = \fCFalse\fP)"

.PP
.nf
Imports an object based on a string\&.  This is useful if you want to
use import paths as endpoints or something similar\&.  An import path can
be specified either in dotted notation (``xml\&.sax\&.saxutils\&.escape``)
or with a colon as object delimiter (``xml\&.sax\&.saxutils:escape``)\&.

If the `silent` is True the return value will be `None` if the import
fails\&.

:return: imported object

.fi
.PP
 
.SS " \fBF\fP jinja2\&.utils\&.internalcode (\fBF\fP f)"

.PP
.nf
Marks the function as internally used
.fi
.PP
 
.SS " bool jinja2\&.utils\&.is_undefined (t\&.Any obj)"

.PP
.nf
Check if the object passed is undefined\&.  This does nothing more than
performing an instance check against :class:`Undefined` but looks nicer\&.
This can be used for custom filters or tests that want to react to
undefined variables\&.  For example a custom default filter can look like
this::

    def default(var, default=''):
        if is_undefined(var):
            return default
        return var

.fi
.PP
 
.SS " str jinja2\&.utils\&.object_type_repr (t\&.Any obj)"

.PP
.nf
Returns the name of the object's type\&.  For some recognized
singletons the name of the object is returned instead\&. (For
example for `None` and `Ellipsis`)\&.

.fi
.PP
 
.SS " t\&.Optional[t\&.IO[t\&.Any]] jinja2\&.utils\&.open_if_exists (str filename, str  mode = \fC'rb'\fP)"

.PP
.nf
Returns a file descriptor for the filename if that file exists,
otherwise ``None``\&.

.fi
.PP
 
.SS " \fBF\fP jinja2\&.utils\&.pass_context (\fBF\fP f)"

.PP
.nf
Pass the :class:`~jinja2\&.runtime\&.Context` as the first argument
to the decorated function when called while rendering a template\&.

Can be used on functions, filters, and tests\&.

If only ``Context\&.eval_context`` is needed, use
:func:`pass_eval_context`\&. If only ``Context\&.environment`` is
needed, use :func:`pass_environment`\&.

\&.\&. versionadded:: 3\&.0\&.0
    Replaces ``contextfunction`` and ``contextfilter``\&.

.fi
.PP
 
.SS " \fBF\fP jinja2\&.utils\&.pass_environment (\fBF\fP f)"

.PP
.nf
Pass the :class:`~jinja2\&.Environment` as the first argument to
the decorated function when called while rendering a template\&.

Can be used on functions, filters, and tests\&.

\&.\&. versionadded:: 3\&.0\&.0
    Replaces ``environmentfunction`` and ``environmentfilter``\&.

.fi
.PP
 
.SS " \fBF\fP jinja2\&.utils\&.pass_eval_context (\fBF\fP f)"

.PP
.nf
Pass the :class:`~jinja2\&.nodes\&.EvalContext` as the first argument
to the decorated function when called while rendering a template\&.
See :ref:`eval-context`\&.

Can be used on functions, filters, and tests\&.

If only ``EvalContext\&.environment`` is needed, use
:func:`pass_environment`\&.

\&.\&. versionadded:: 3\&.0\&.0
    Replaces ``evalcontextfunction`` and ``evalcontextfilter``\&.

.fi
.PP
 
.SS " str jinja2\&.utils\&.pformat (t\&.Any obj)"

.PP
.nf
Format an object using :func:`pprint\&.pformat`\&.
.fi
.PP
 
.SS " t\&.Callable[[t\&.Optional[str]], bool] jinja2\&.utils\&.select_autoescape (t\&.Collection[str]  enabled_extensions = \fC('html', 'htm', 'xml')\fP, t\&.Collection[str]  disabled_extensions = \fC()\fP, bool  default_for_string = \fCTrue\fP, bool  default = \fCFalse\fP)"

.PP
.nf
Intelligently sets the initial value of autoescaping based on the
filename of the template\&.  This is the recommended way to configure
autoescaping if you do not want to write a custom function yourself\&.

If you want to enable it for all templates created from strings or
for all templates with `\&.html` and `\&.xml` extensions::

    from jinja2 import Environment, select_autoescape
    env = Environment(autoescape=select_autoescape(
        enabled_extensions=('html', 'xml'),
        default_for_string=True,
    ))

Example configuration to turn it on at all times except if the template
ends with `\&.txt`::

    from jinja2 import Environment, select_autoescape
    env = Environment(autoescape=select_autoescape(
        disabled_extensions=('txt',),
        default_for_string=True,
        default=True,
    ))

The `enabled_extensions` is an iterable of all the extensions that
autoescaping should be enabled for\&.  Likewise `disabled_extensions` is
a list of all templates it should be disabled for\&.  If a template is
loaded from a string then the default from `default_for_string` is used\&.
If nothing matches then the initial value of autoescaping is set to the
value of `default`\&.

For security reasons this function operates case insensitive\&.

\&.\&. versionadded:: 2\&.9

.fi
.PP
 
.SS " str jinja2\&.utils\&.url_quote (t\&.Any obj, str  charset = \fC'utf\-8'\fP, bool  for_qs = \fCFalse\fP)"

.PP
.nf
Quote a string for use in a URL using the given charset\&.

:param obj: String or bytes to quote\&. Other types are converted to
    string then encoded to bytes using the given charset\&.
:param charset: Encode text to bytes using this charset\&.
:param for_qs: Quote '/' and use '+' for spaces\&.

.fi
.PP
 
.SS " str jinja2\&.utils\&.urlize (str text, t\&.Optional[int]  trim_url_limit = \fCNone\fP, t\&.Optional[str]  rel = \fCNone\fP, t\&.Optional[str]  target = \fCNone\fP, t\&.Optional[t\&.Iterable[str]]  extra_schemes = \fCNone\fP)"

.PP
.nf
Convert URLs in text into clickable links\&.

This may not recognize links in some situations\&. Usually, a more
comprehensive formatter, such as a Markdown library, is a better
choice\&.

Works on ``http://``, ``https://``, ``www\&.``, ``mailto:``, and email
addresses\&. Links with trailing punctuation (periods, commas, closing
parentheses) and leading punctuation (opening parentheses) are
recognized excluding the punctuation\&. Email addresses that include
header fields are not recognized (for example,
``mailto:address@example\&.com?cc=copy@example\&.com``)\&.

:param text: Original text containing URLs to link\&.
:param trim_url_limit: Shorten displayed URL values to this length\&.
:param target: Add the ``target`` attribute to links\&.
:param rel: Add the ``rel`` attribute to links\&.
:param extra_schemes: Recognize URLs that start with these schemes
    in addition to the default behavior\&.

\&.\&. versionchanged:: 3\&.0
    The ``extra_schemes`` parameter was added\&.

\&.\&. versionchanged:: 3\&.0
    Generate ``https://`` links for URLs without a scheme\&.

\&.\&. versionchanged:: 3\&.0
    The parsing rules were updated\&. Recognize email addresses with
    or without the ``mailto:`` scheme\&. Validate IP addresses\&. Ignore
    parentheses and brackets in more cases\&.

.fi
.PP
 
.SH "Variable Documentation"
.PP 
.SS "jinja2\&.utils\&._email_re = re\&.compile(r'^\\S+@\\w[\\w\&.\-]*\\\&.\\w+$')\fC [protected]\fP"

.SS "jinja2\&.utils\&._http_re\fC [protected]\fP"
\fBInitial value:\fP.PP
.nf
1 =  re\&.compile(
2     ,
3     re\&.IGNORECASE | re\&.VERBOSE,
4 )
.fi

.SS "str jinja2\&.utils\&.concat = ''\&.join"

.SS "jinja2\&.utils\&.F = t\&.TypeVar('F', bound=t\&.Callable[\&.\&.\&., t\&.Any])"

.SH "Author"
.PP 
Generated automatically by Doxygen for Minilog from the source code\&.
